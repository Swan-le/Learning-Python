# VARIABLES (This is a memory location where you can type in the name, and get the number it is = to.
    #student_count = 1000 #integer
    #rating = 4.99 #float (aka, a floating point number)
    #is_published = True  #Example of Boolen values (These can either be true or false, nothing else)
    #course_name = "Python training course" #String (AKA a text, this needs " " to be a string
    #full = f"{course_name} {rating}" #FORMATTED STRING (Used to connect variables togeather)
x = input("x:")

# FUNCTIONS (Used to preform various tasks, like a TV Remote)
    # len()  #number of charecters in this string.

#Example:
y = int(x) + 1
print(f"x: {x},y: {y}")
